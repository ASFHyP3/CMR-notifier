name: Test
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  pixi-tests:
    uses: ASFHyP3/actions/.github/workflows/reusable-pixi-tasks.yml@v0.22.0
    with:
      tasks: >-
        ["tests"]
    permissions:
      contents: read
